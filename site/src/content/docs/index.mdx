---
title: reklawdbox
description: MCP server for Rekordbox 7.x — AI-assisted library management, genre classification, and set building.
template: splash
hero:
  tagline: AI-assisted Rekordbox library management via MCP. Read your encrypted database directly. Stage metadata changes safely. Export Rekordbox-compatible XML.
  image:
    file: ../../assets/logo.png
    alt: reklawdbox logo — a crab gripping a vinyl record
  actions:
    - text: Get Started
      link: /getting-started/
      icon: right-arrow
    - text: GitHub
      link: https://github.com/ryan-voitiskis/reklawdbox
      icon: external
      variant: minimal
---

import { Card, CardGrid, LinkCard } from '@astrojs/starlight/components';

Manage your Rekordbox library with AI through the
[Model Context Protocol](https://modelcontextprotocol.io/).
reklawdbox connects any MCP-compatible host — Claude Code, Claude Desktop,
Codex — to your collection.

<CardGrid>
  <LinkCard title="Getting Started" href="/getting-started/" description="Install, configure, and connect reklawdbox to your MCP host." />
  <LinkCard title="Concepts" href="/concepts/" description="Architecture, safety model, and how harmonic mixing works." />
  <LinkCard title="Workflows" href="/workflows/genre-classification/" description="Genre classification, set building, and collection audits." />
  <LinkCard title="MCP Tools" href="/mcp-tools/" description="All 30 tools grouped by function — library, enrichment, staging, mixing, and more." />
  <LinkCard title="CLI" href="/cli/" description="Standalone commands for batch enrichment, audio analysis, and file tag management." />
  <LinkCard title="Reference" href="/reference/" description="Environment variables, transition scoring math, and XML export details." />
  <LinkCard title="Troubleshooting" href="/troubleshooting/" description="Fix common setup, auth, and runtime problems." />
</CardGrid>

## What reklawdbox does

reklawdbox is an MCP server that sits between your AI agent and Rekordbox 7.x.
It gives the agent structured access to your library while keeping your
database safe. All metadata changes are staged in memory and exported as
XML — the Rekordbox database is never modified directly.

- **Reads the encrypted Rekordbox `master.db` directly** using SQLCipher.
  The database is opened read-only — reklawdbox never writes to it.
- **Stages metadata changes in memory** (genre, comments, rating, color)
  so you can review them before anything is exported.
- **Exports Rekordbox-compatible XML** for safe reimport.
  Your database is never modified directly.
- **Enriches tracks via Discogs and Beatport** — pulls release metadata,
  labels, catalog numbers, and artwork through a broker API and HTML extraction.
- **Analyzes audio with stratum-dsp and optional Essentia** — computes BPM,
  key, energy, danceability, and spectral features from your actual audio files.
- **Scores transitions and builds DJ set sequences** — uses harmonic
  compatibility, BPM proximity, energy curves, and genre affinity to find
  optimal track ordering.
- **Provides CLI subcommands** for batch audio analysis and native file tag
  read/write outside of MCP.

## 30 MCP tools

**Library & Data** — `read_library`, `search_tracks`, `get_track`, `get_playlists`, `get_playlist_tracks`, `resolve_track_data`, `resolve_tracks_data`, `cache_coverage`

**Enrichment & Analysis** — `lookup_discogs`, `lookup_beatport`, `enrich_tracks`, `analyze_track_audio`, `analyze_audio_batch`, `setup_essentia`

**Classification & Staging** — `get_genre_taxonomy`, `suggest_normalizations`, `update_tracks`, `preview_changes`, `write_xml`, `clear_changes`

**Mixing & Sequencing** — `score_transition`, `query_transition_candidates`, `build_set`

**Files & System** — `read_file_tags`, `write_file_tags`, `extract_cover_art`, `embed_cover_art`, `audit_state`, `clear_caches`, `help`

## 6 CLI commands

`hydrate`, `analyze`, `read-tags`, `write-tags`, `extract-art`, `embed-art`
